import styled from 'styled-components'

export const ContainerIcon = styled.button`
  width: 40px;
  height: 36px;
  border: none;
  outline: transparent;
  background: var(--primary);
  cursor: pointer;
  border-radius: 10px;
`